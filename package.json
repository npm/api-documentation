{
  "name": "openapi-documentation",
  "version": "1.0.0",
  "description": "npm registry API documentation build for GitHub Pages",
  "devDependencies": {
    "@redocly/cli": "^1.34.4",
    "openapi-merge-cli": "^1.3.2"
  },
  "scripts": {
    "merge": "cd api && npx openapi-merge-cli -c merge-config.yaml",
    "build": "npm run merge && npx @redocly/cli build-docs api/api.yaml -o html/index.html"
  }
}
